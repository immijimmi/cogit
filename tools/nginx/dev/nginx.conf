worker_processes 1;

events {
    worker_connections 1024;
}

http {
    include mime.types;
    include conf.d/*.conf;

    sendfile on;

    error_log logs/error.log debug;
    access_log logs/access.log;
}
